import { NodeEntry } from 'slate';
import { BlocksStore } from '../BlocksEditor';
export declare const decorateCode: (_entry: NodeEntry) => never[];
declare const codeBlocks: Pick<BlocksStore, 'code'>;
export { codeBlocks };
