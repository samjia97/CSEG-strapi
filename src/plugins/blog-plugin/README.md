# blog-plugin

Adds a **Blogs** link to the Strapi admin left navigation, pointing at the
`blog` collection type via the shared `custom-content-manager3` view.

Cloned from `forum-plugin` — differs only in the plugin id, the menu label,
and the `to` target (`api::blog.blog`).
